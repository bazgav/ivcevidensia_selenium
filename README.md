
# IVCEVIDENSIA Interview technical assessment

This is Java based project for running automation tests based on Selenium and Junit.

The project contains simple maven configuration that adds the required dependencies (Junit and Selenium)

Requirements to run the project: Java 21, Maven

To run the project, run: `mvn test` command inside the project. 

